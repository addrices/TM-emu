#include <stdbool.h>
#pragma once

char* input;         //turing machine's input string 
char* tm;            //turing machine file
bool h_flag;         //if need help
bool v_flag;         //if need verbose
bool d_flag;        //if need debug